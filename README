SCMDWeb README

[Folder Contents]
src -- Java source files
test -- JUnit test files

scmd -- warファイルとして固めるフォルダ
scmd/WEB-INF --　JSP/Servlet用の設定ファイル
scmd/resources -- 画像、stylesheet等、静的なファイルの置き場　

web　serverの構成は、scmd/resources内以外はTomcatに処理させ、
（*.jsp, *.tld, *.xml, *.do(servlet)等はTomcat）
resources内は、apacheに処理を任せるのが基本

性能に信頼を置けるなら、resources内もtomcatに任せてしまって良い
